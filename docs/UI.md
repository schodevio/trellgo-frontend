# UI Guidelines

Styling uses Tailwind CSS v4.

Components should be built using PrimeVue when possible.

Examples:

- Button → PrimeVue Button
- Input → PrimeVue InputText
- Dialog → PrimeVue Dialog
- Toast → PrimeVue Toast
