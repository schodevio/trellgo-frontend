# Code Guidelines

- use TypeScript everywhere.
- composables start with `use*` like:
  - useAuth
  - useBoards
- components should be small and reusable.
- business logic should stay inside composables.
